package ke.hub.rentalhubke.di

import org.koin.dsl.module

val appModule = module {
    // Define your Koin modules here
    // For example, you can define a service or repository
    // single { MyService() }
    // single { MyRepository(get()) }
}